package com.kalloer1.p2p.channel;

/** What a channel transports. Mutable at runtime — switchable between ITEM / FLUID / ENERGY from the channel detail screen. */
public enum ChannelType {
    ITEM, FLUID, ENERGY
}
